namespace KakarotMod.KakarotCode.Patches;

// Deprecated on purpose.
// We no longer force custom merchant/rest-site scene paths because that can break
// if scene-script classes are unavailable in a stale deployment.
public static class KakarotRoomScenePathPatch
{
}
